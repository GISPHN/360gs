# Upstream refinement context

The c9 browser path remains isolated from upstream Brush's full refinement behavior. Upstream multi-view consistent densification and pruning is still being developed separately; c9 should therefore be treated as a constrained browser implementation whose quality is assessed by field metrics rather than assumed from upstream parity.
