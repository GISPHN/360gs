# v0.3c9 design note

The browser growth path intentionally favors bounded execution over full Brush refinement. It is an experimental browser-specific approximation, not a claim of equivalence to upstream gradient-based densification. Its effect must be evaluated empirically using the retained training-view and grouped held-out metrics.
