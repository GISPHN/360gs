# v0.3c9 metrics note

The interrupted c8 values at iteration 1,580 are intermediate metrics rather than a final quality result. They should not be compared directly with completed c7 metrics. The next field run should first establish that growth passes the former timeout boundary, then compare final training and held-out PSNR/SSIM.
