const baSourceVideo = document.querySelector('#source-video');
const baProgressText = document.querySelector('#progress-text');

let baGeneration = 0;
let baRunning = false;
let baLastSignature = '';

const BA_VIEW_SIZE = 176;
const BA_FOV_DEG = 100;
const BA_MAX_TRACKS = 260;
const BA_MAX_OBSERVATIONS = 1400;
const BA_MIN_TRACK_LENGTH = 3;
const BA_MAX_ITERATIONS = 7;
const BA_HUBER_DEG = 2.0;
const BA_OUTLIER_DEG = 8.0;
const BA_POINT_EPS = 1e-4;
const BA_ROT_EPS = 2e-5;
const BA_TRANS_EPS = 1e-4;

function baClamp(value, min, max) { return Math.max(min, Math.min(max, value)); }
function baNorm3(v) { return Math.hypot(v[0], v[1], v[2]); }
function baNormalize3(v) { const n = baNorm3(v); return !Number.isFinite(n) || n < 1e-12 ? [0, 0, 0] : [v[0] / n, v[1] / n, v[2] / n]; }
function baDot3(a, b) { return a[0] * b[0] + a[1] * b[1] + a[2] * b[2]; }
function baAdd3(a, b) { return [a[0] + b[0], a[1] + b[1], a[2] + b[2]]; }
function baSub3(a, b) { return [a[0] - b[0], a[1] - b[1], a[2] - b[2]]; }
function baScale3(v, s) { return [v[0] * s, v[1] * s, v[2] * s]; }
function baCross3(a, b) { return [a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]]; }
function baIdentity3() { return [1, 0, 0, 0, 1, 0, 0, 0, 1]; }
function baTranspose3(m) { return [m[0], m[3], m[6], m[1], m[4], m[7], m[2], m[5], m[8]]; }
function baMatVec3(m, v) { return [m[0] * v[0] + m[1] * v[1] + m[2] * v[2], m[3] * v[0] + m[4] * v[1] + m[5] * v[2], m[6] * v[0] + m[7] * v[1] + m[8] * v[2]]; }
function baMul3(a, b) { const out = new Array(9).fill(0); for (let r = 0; r < 3; r += 1) for (let c = 0; c < 3; c += 1) out[r * 3 + c] = a[r * 3] * b[c] + a[r * 3 + 1] * b[3 + c] + a[r * 3 + 2] * b[6 + c]; return out; }
function baMedian(values) { if (!values.length) return 0; const sorted = [...values].sort((a, b) => a - b), mid = Math.floor(sorted.length / 2); return sorted.length % 2 ? sorted[mid] : (sorted[mid - 1] + sorted[mid]) / 2; }

function baSolveLinear(matrix, rhs, n) { const a = Array.from(matrix), b = Array.from(rhs); for (let col = 0; col < n; col += 1) { let pivot = col; for (let r = col + 1; r < n; r += 1) if (Math.abs(a[r * n + col]) > Math.abs(a[pivot * n + col])) pivot = r; if (Math.abs(a[pivot * n + col]) < 1e-10) return null; if (pivot !== col) { for (let c = col; c < n; c += 1) [a[col * n + c], a[pivot * n + c]] = [a[pivot * n + c], a[col * n + c]]; [b[col], b[pivot]] = [b[pivot], b[col]]; } const div = a[col * n + col]; for (let c = col; c < n; c += 1) a[col * n + c] /= div; b[col] /= div; for (let r = 0; r < n; r += 1) { if (r === col) continue; const f = a[r * n + col]; if (Math.abs(f) < 1e-14) continue; for (let c = col; c < n; c += 1) a[r * n + c] -= f * a[col * n + c]; b[r] -= f * b[col]; } } return b; }
function baExpSO3(delta) { const theta = baNorm3(delta); if (theta < 1e-12) return baIdentity3(); const [x, y, z] = delta.map((v) => v / theta), c = Math.cos(theta), s = Math.sin(theta), t = 1 - c; return [t*x*x+c, t*x*y-s*z, t*x*z+s*y, t*x*y+s*z, t*y*y+c, t*y*z-s*x, t*x*z-s*y, t*y*z+s*x, t*z*z+c]; }
function baBearing(x, y, yawDeg) { const halfFov = Math.tan((BA_FOV_DEG * Math.PI / 180) / 2), nx = ((x + 0.5) / BA_VIEW_SIZE * 2 - 1) * halfFov, ny = ((y + 0.5) / BA_VIEW_SIZE * 2 - 1) * halfFov; const local = baNormalize3([nx, -ny, 1]), yaw = yawDeg * Math.PI / 180, c = Math.cos(yaw), s = Math.sin(yaw); return baNormalize3([local[0] * c + local[2] * s, local[1], -local[0] * s + local[2] * c]); }
function baDescriptorDistance(a, b) { let sum = 0; for (let i = 0; i < a.length; i += 1) { const d = a[i] - b[i]; sum += d * d; } return sum; }
function baFlattenFrame(frame, frameIndex) { const observations = []; frame.views.forEach((view, viewIndex) => { view.features.forEach((feature, featureIndex) => observations.push({ key: `${frameIndex}:${viewIndex}:${featureIndex}`, frameIndex, viewIndex, featureIndex, descriptor: feature.descriptor, response: feature.response || 0, bearing: baBearing(feature.x, feature.y, view.yaw) })); }); return observations; }
function baNearestTwo(source, targets) { let bestIndex = -1, best = Infinity, second = Infinity; for (let i = 0; i < targets.length; i += 1) { const d = baDescriptorDistance(source.descriptor, targets[i].descriptor); if (d < best) { second = best; best = d; bestIndex = i; } else if (d < second) second = d; } return { bestIndex, best, second }; }
function baMatchFrames(left, right) { if (left.length < 10 || right.length < 10) return []; const forward = left.map((x) => baNearestTwo(x, right)), reverse = right.map((x) => baNearestTwo(x, left)), matches = []; forward.forEach((m, i) => { if (m.bestIndex < 0 || !Number.isFinite(m.second) || m.best > m.second * 0.74) return; const back = reverse[m.bestIndex]; if (!back || back.bestIndex !== i || !Number.isFinite(back.second) || back.best > back.second * 0.80) return; matches.push({ left: left[i], right: right[m.bestIndex], quality: m.best }); }); return matches; }
class BAUnionFind { constructor() { this.parent = new Map(); } add(x) { if (!this.parent.has(x)) this.parent.set(x, x); } find(x) { const p = this.parent.get(x); if (p === x) return x; const root = this.find(p); this.parent.set(x, root); return root; } union(a, b) { this.add(a); this.add(b); const ra = this.find(a), rb = this.find(b); if (ra !== rb) this.parent.set(rb, ra); } }
function baBuildTracks(frames) { const flattened = frames.map(baFlattenFrame), uf = new BAUnionFind(), lookup = new Map(); flattened.flat().forEach((obs) => { uf.add(obs.key); lookup.set(obs.key, obs); }); const connect = (i, j) => { const matches = baMatchFrames(flattened[i], flattened[j]); for (const match of matches) uf.union(match.left.key, match.right.key); }; for (let i = 0; i < frames.length - 1; i += 1) connect(i, i + 1); for (let i = 0; i < frames.length - 2; i += 1) connect(i, i + 2); const groups = new Map(); for (const obs of lookup.values()) { const root = uf.find(obs.key); if (!groups.has(root)) groups.set(root, []); groups.get(root).push(obs); } let tracks = []; for (const observations of groups.values()) { const byFrame = new Map(); for (const obs of observations) { const existing = byFrame.get(obs.frameIndex); if (!existing || obs.response > existing.response) byFrame.set(obs.frameIndex, obs); } const unique = [...byFrame.values()].sort((a, b) => a.frameIndex - b.frameIndex); if (unique.length < BA_MIN_TRACK_LENGTH) continue; const score = unique.length * 100000 + unique.reduce((s, x) => s + x.response, 0) / unique.length; tracks.push({ observations: unique, score }); } tracks.sort((a, b) => b.score - a.score); tracks = tracks.slice(0, BA_MAX_TRACKS); let observations = tracks.reduce((s, t) => s + t.observations.length, 0); while (observations > BA_MAX_OBSERVATIONS && tracks.length > 20) { const removed = tracks.pop(); observations -= removed.observations.length; } return tracks; }
function baClonePoses(cameraPoses) { return cameraPoses.map((pose) => ({ position: [...pose.position], cameraToWorld: [...pose.cameraToWorld], time: pose.time, valid: pose.valid !== false })); }
function baRayWorld(pose, bearing) { return baNormalize3(baMatVec3(pose.cameraToWorld, bearing)); }
function baTriangulateTrack(track, poses) { const A = new Array(9).fill(0), b = [0, 0, 0]; let valid = 0; for (const obs of track.observations) { const pose = poses[obs.frameIndex]; if (!pose) continue; const d = baRayWorld(pose, obs.bearing), C = pose.position; const M = [1-d[0]*d[0], -d[0]*d[1], -d[0]*d[2], -d[1]*d[0], 1-d[1]*d[1], -d[1]*d[2], -d[2]*d[0], -d[2]*d[1], 1-d[2]*d[2]]; for (let i = 0; i < 9; i += 1) A[i] += M[i]; const MC = baMatVec3(M, C); b[0] += MC[0]; b[1] += MC[1]; b[2] += MC[2]; valid += 1; } if (valid < BA_MIN_TRACK_LENGTH) return null; const point = baSolveLinear(A, b, 3); if (!point || !point.every(Number.isFinite)) return null; let positive = 0; const depths = []; for (const obs of track.observations) { const pose = poses[obs.frameIndex], d = baRayWorld(pose, obs.bearing), depth = baDot3(d, baSub3(point, pose.position)); if (depth > 0) positive += 1; depths.push(depth); } if (positive / valid < 0.67 || baMedian(depths) <= 0 || baMedian(depths) > 80) return null; return point; }
function baInitializeTracks(rawTracks, poses) { const tracks = []; for (const track of rawTracks) { const point = baTriangulateTrack(track, poses); if (point) tracks.push({ ...track, position: point }); } return tracks; }
function baPredictedBearing(pose, point) { const world = baNormalize3(baSub3(point, pose.position)); return baNormalize3(baMatVec3(baTranspose3(pose.cameraToWorld), world)); }
function baObservation(pose, point, observed) { const pred = baPredictedBearing(pose, point), dot = baClamp(baDot3(pred, observed), -1, 1), angle = Math.acos(dot); return { residual: [pred[0]-observed[0], pred[1]-observed[1], pred[2]-observed[2]], angle }; }
function baHuberWeight(angle) { const delta = BA_HUBER_DEG * Math.PI / 180; if (angle <= delta || angle < 1e-12) return 1; return delta / angle; }
function baHuberCost(angle) { const delta = BA_HUBER_DEG * Math.PI / 180; return angle <= delta ? 0.5 * angle * angle : delta * (angle - 0.5 * delta); }
function baTrackCost(track, poses, point = track.position) { let cost = 0; for (const obs of track.observations) cost += baHuberCost(baObservation(poses[obs.frameIndex], point, obs.bearing).angle); return cost; }
function baCameraCost(cameraIndex, tracks, poses, pose = poses[cameraIndex]) { let cost = 0; for (const track of tracks) for (const obs of track.observations) if (obs.frameIndex === cameraIndex) cost += baHuberCost(baObservation(pose, track.position, obs.bearing).angle); return cost; }
function baPerturbPose(pose, parameter, epsilon) { const next = { position: [...pose.position], cameraToWorld: [...pose.cameraToWorld], time: pose.time, valid: pose.valid }; if (parameter < 3) { const delta = [0, 0, 0]; delta[parameter] = epsilon; next.cameraToWorld = baMul3(next.cameraToWorld, baExpSO3(delta)); } else next.position[parameter - 3] += epsilon; return next; }
function baAccumulateNormal(J, residual, weight, n, H, g) { const sw = Math.sqrt(Math.max(1e-8, weight)); for (let r = 0; r < 3; r += 1) { const rr = residual[r] * sw; for (let c = 0; c < n; c += 1) { const jc = J[r][c] * sw; g[c] += jc * rr; for (let d = c; d < n; d += 1) H[c * n + d] += jc * J[r][d] * sw; } } for (let c = 0; c < n; c += 1) for (let d = 0; d < c; d += 1) H[c * n + d] = H[d * n + c]; }
function baRefinePoint(track, poses, lambda) { const H = new Array(9).fill(0), g = [0, 0, 0], point = track.position; for (const obs of track.observations) { const pose = poses[obs.frameIndex], base = baObservation(pose, point, obs.bearing), J = [[], [], []]; for (let p = 0; p < 3; p += 1) { const shifted = [...point]; shifted[p] += BA_POINT_EPS; const pert = baObservation(pose, shifted, obs.bearing).residual; for (let r = 0; r < 3; r += 1) J[r][p] = (pert[r] - base.residual[r]) / BA_POINT_EPS; } baAccumulateNormal(J, base.residual, baHuberWeight(base.angle), 3, H, g); } for (let i = 0; i < 3; i += 1) H[i * 3 + i] += lambda; const step = baSolveLinear(H, g.map((x) => -x), 3); if (!step) return false; const maxStep = Math.max(0.05, Math.min(0.45, baNorm3(point) * 0.08)), n = baNorm3(step), scaled = n > maxStep ? baScale3(step, maxStep / n) : step; const proposal = baAdd3(point, scaled), before = baTrackCost(track, poses), after = baTrackCost(track, poses, proposal); if (Number.isFinite(after) && after < before) { track.position = proposal; return true; } return false; }
function baRefineCamera(cameraIndex, tracks, poses, lambda, fixed) { if (fixed.has(cameraIndex)) return false; const relevant = []; for (const track of tracks) for (const obs of track.observations) if (obs.frameIndex === cameraIndex) relevant.push({ track, obs }); if (relevant.length < 8) return false; const H = new Array(36).fill(0), g = new Array(6).fill(0), pose = poses[cameraIndex]; for (const item of relevant) { const base = baObservation(pose, item.track.position, item.obs.bearing), J = [[], [], []]; for (let p = 0; p < 6; p += 1) { const eps = p < 3 ? BA_ROT_EPS : BA_TRANS_EPS, pertPose = baPerturbPose(pose, p, eps), pert = baObservation(pertPose, item.track.position, item.obs.bearing).residual; for (let r = 0; r < 3; r += 1) J[r][p] = (pert[r] - base.residual[r]) / eps; } baAccumulateNormal(J, base.residual, baHuberWeight(base.angle), 6, H, g); } for (let i = 0; i < 6; i += 1) H[i * 6 + i] += lambda; const step = baSolveLinear(H, g.map((x) => -x), 6); if (!step) return false; let rot = step.slice(0, 3), trans = step.slice(3), rn = baNorm3(rot), tn = baNorm3(trans); const maxRot = 2.0 * Math.PI / 180, maxTrans = 0.18; if (rn > maxRot) rot = baScale3(rot, maxRot / rn); if (tn > maxTrans) trans = baScale3(trans, maxTrans / tn); const proposal = { ...pose, cameraToWorld: baMul3(pose.cameraToWorld, baExpSO3(rot)), position: baAdd3(pose.position, trans) }; const before = baCameraCost(cameraIndex, tracks, poses), after = baCameraCost(cameraIndex, tracks, poses, proposal); if (Number.isFinite(after) && after < before) { poses[cameraIndex] = proposal; return true; } return false; }
function baMetrics(tracks, poses) { const angles = []; for (const track of tracks) for (const obs of track.observations) { const angle = baObservation(poses[obs.frameIndex], track.position, obs.bearing).angle * 180 / Math.PI; if (Number.isFinite(angle)) angles.push(angle); } const usable = angles.filter((a) => a <= BA_OUTLIER_DEG), rms = usable.length ? Math.sqrt(usable.reduce((s, a) => s + a * a, 0) / usable.length) : 0; return { count: angles.length, inlierCount: usable.length, medianDeg: baMedian(usable), rmsDeg: rms, outlierRate: 1 - usable.length / Math.max(1, angles.length) }; }
function baAnchorIndices(poses) { const fixed = new Set([0]), origin = poses[0]?.position || [0, 0, 0]; let anchor = 1; for (let i = 1; i < poses.length; i += 1) { if (baNorm3(baSub3(poses[i].position, origin)) > 0.2) { anchor = i; break; } } fixed.add(Math.min(anchor, Math.max(0, poses.length - 1))); return fixed; }
function baOptimize(rawResult) { const poses = baClonePoses(rawResult.geometry.cameraPoses), rawTracks = baBuildTracks(rawResult.frames), tracks = baInitializeTracks(rawTracks, poses); if (tracks.length < 8) return { success: false, quality: 'hold', reason: '複数フレームで追跡できる3D点が不足しています。', poses, tracks, initial: baMetrics(tracks, poses), final: baMetrics(tracks, poses), iterations: 0 }; const fixed = baAnchorIndices(poses), initial = baMetrics(tracks, poses); let lambda = 2e-3, iterations = 0; for (let iter = 0; iter < BA_MAX_ITERATIONS; iter += 1) { let accepted = 0; for (const track of tracks) if (baRefinePoint(track, poses, lambda)) accepted += 1; for (let cameraIndex = 0; cameraIndex < poses.length; cameraIndex += 1) if (baRefineCamera(cameraIndex, tracks, poses, lambda, fixed)) accepted += 1; iterations = iter + 1; const current = baMetrics(tracks, poses); if (accepted === 0) { lambda *= 3; if (lambda > 0.2) break; } else lambda = Math.max(2e-5, lambda * 0.65); if (current.medianDeg < 0.18 || (iter >= 2 && accepted < Math.max(2, tracks.length * 0.03))) break; } const final = baMetrics(tracks, poses), improvement = initial.medianDeg > 1e-6 ? (initial.medianDeg - final.medianDeg) / initial.medianDeg : 0; let quality = 'hold'; if (tracks.length >= 12 && final.inlierCount >= 35 && final.medianDeg <= Math.max(2.2, initial.medianDeg * 0.95) && final.outlierRate <= 0.38) quality = 'candidate'; if (tracks.length >= 24 && final.inlierCount >= 80 && final.medianDeg <= 1.5 && final.outlierRate <= 0.25 && (improvement >= 0.08 || final.medianDeg <= 0.75)) quality = 'good'; return { success: quality !== 'hold', quality, poses, tracks, initial, final, iterations, improvement, fixed: [...fixed] }; }
function baEnsurePanel() { let panel = document.querySelector('#ba-panel'); if (panel) return panel; const sfmPanel = document.querySelector('#sfm-panel'); if (!sfmPanel) return null; panel = document.createElement('section'); panel.id = 'ba-panel'; panel.className = 'ba-panel'; panel.hidden = true; panel.innerHTML = `<div class="ba-heading"><div><p class="eyebrow">全体最適化</p><h3>カメラ姿勢と疎3D点をまとめて調整</h3></div><span class="ba-auto">自動設定</span></div><p class="ba-description">3フレーム以上で追跡できる特徴だけを使い、360°方向ベクトルの観測誤差を小さくするようにカメラ姿勢と3D点を同じ目的関数で交互に最適化します。外れ値の影響を抑えるロバスト損失を使い、最初の2カメラを固定して任意スケールを保持します。</p><div class="ba-stats"><div><span>最適化した区間</span><strong id="ba-count">—</strong></div><div><span>良好</span><strong id="ba-good">—</strong></div><div><span>複数視点トラック</span><strong id="ba-tracks">—</strong></div><div><span>観測点</span><strong id="ba-observations">—</strong></div></div><div id="ba-list" class="ba-list"></div><div id="ba-message" class="message-box" hidden></div><p class="ba-note">これはブラウザ向け軽量Bundle Adjustmentです。Ceres等の大規模Schur solverそのものではなく、同じロバストな再投影誤差目的を小規模区間に対してブロック交互最適化しています。絶対距離スケールは引き続き未確定です。</p>`; sfmPanel.insertAdjacentElement('afterend', panel); return panel; }
function baDrawPreview(canvas, poses, tracks) { const dpr = Math.min(2, window.devicePixelRatio || 1), width = Math.max(300, canvas.clientWidth || 520), height = 220; canvas.width = width * dpr; canvas.height = height * dpr; const ctx = canvas.getContext('2d'); ctx.scale(dpr, dpr); ctx.fillStyle = '#fff'; ctx.fillRect(0, 0, width, height); ctx.strokeStyle = '#d7dde7'; ctx.strokeRect(.5, .5, width-1, height-1); const cameraPoints = poses.map((p) => p.position), cloud = tracks.map((t) => t.position), all = [...cameraPoints, ...cloud]; if (!all.length) return; let minX = Math.min(...all.map((p) => p[0])), maxX = Math.max(...all.map((p) => p[0])), minZ = Math.min(...all.map((p) => p[2])), maxZ = Math.max(...all.map((p) => p[2])); if (maxX-minX < 1e-4) { minX -= 1; maxX += 1; } if (maxZ-minZ < 1e-4) { minZ -= 1; maxZ += 1; } const pad = 20, scale = Math.min((width-2*pad)/(maxX-minX), (height-2*pad)/(maxZ-minZ)), xy = (p) => [pad+(p[0]-minX)*scale, height-pad-(p[2]-minZ)*scale]; ctx.fillStyle = 'rgba(75,85,99,.30)'; for (const p of cloud) { const [x,y] = xy(p); ctx.fillRect(x-1,y-1,2,2); } if (cameraPoints.length > 1) { ctx.strokeStyle = '#111827'; ctx.lineWidth = 2; ctx.beginPath(); cameraPoints.forEach((p,i) => { const [x,y] = xy(p); if (!i) ctx.moveTo(x,y); else ctx.lineTo(x,y); }); ctx.stroke(); } cameraPoints.forEach((p,i) => { const [x,y] = xy(p); ctx.beginPath(); ctx.arc(x,y,i===0||i===cameraPoints.length-1?4:2.5,0,Math.PI*2); ctx.fillStyle = i===0?'#16803c':i===cameraPoints.length-1?'#b45309':'#111827'; ctx.fill(); }); }
function baRender(results) { const panel = baEnsurePanel(); if (!panel) return; panel.hidden = false; const good = results.filter((r) => r.optimization.quality === 'good'), usable = results.filter((r) => r.optimization.quality !== 'hold'); const trackCount = results.reduce((s,r) => s + r.optimization.tracks.length, 0), obsCount = results.reduce((s,r) => s + r.optimization.final.count, 0); panel.querySelector('#ba-count').textContent = `${results.length}区間`; panel.querySelector('#ba-good').textContent = good.length ? `${good.length}区間` : 'なし'; panel.querySelector('#ba-tracks').textContent = trackCount.toLocaleString(); panel.querySelector('#ba-observations').textContent = obsCount.toLocaleString(); const list = panel.querySelector('#ba-list'); list.replaceChildren(); for (const item of results) { const opt = item.optimization, card = document.createElement('article'); card.className = `ba-card ${opt.quality}`; const label = opt.quality === 'good' ? '最適化良好' : opt.quality === 'candidate' ? '最適化候補' : '保留', before = opt.initial, after = opt.final; card.innerHTML = `<div class="ba-card-head"><div><span>候補区間 ${item.source.segment.id}</span><h4>${item.source.segment.start.toFixed(1)}秒 〜 ${item.source.segment.end.toFixed(1)}秒</h4></div><span class="ba-badge ${opt.quality}">${label}</span></div><div class="ba-metrics"><span>複数視点トラック <strong>${opt.tracks.length}</strong></span><span>観測 <strong>${after.count}</strong></span><span>反復 <strong>${opt.iterations}</strong></span><span>角度誤差中央値 <strong>${before.medianDeg.toFixed(2)}° → ${after.medianDeg.toFixed(2)}°</strong></span><span>RMS角度誤差 <strong>${before.rmsDeg.toFixed(2)}° → ${after.rmsDeg.toFixed(2)}°</strong></span><span>外れ観測 <strong>${Math.round(after.outlierRate*100)}%</strong></span></div><canvas class="ba-preview"></canvas><p>${opt.quality === 'good' ? '複数視点の観測を同時に使っても、カメラ軌跡と疎点群が安定しています。3DGS学習用データへ進める候補です。' : opt.quality === 'candidate' ? '誤差は改善しましたが、3DGS入力前に追加の軌跡整合または再訪拘束を確認します。' : opt.reason || '複数視点での追跡または最適化後の誤差が十分に安定しませんでした。'}</p>`; list.append(card); baDrawPreview(card.querySelector('canvas'), opt.poses, opt.tracks); } const message = panel.querySelector('#ba-message'); message.hidden = false; if (good.length) { message.className = 'message-box success'; message.textContent = `${good.length}区間で軽量Bundle Adjustment後も幾何が安定しました。次は3DGS学習用のカメラ姿勢・画像・疎点群を整理し、ブラウザ学習エンジンへ接続します。`; } else if (usable.length) { message.className = 'message-box warning'; message.textContent = '全体最適化で改善する区間はあります。次に再訪位置や長距離対応を追加して、3DGS入力前の軌跡をさらに安定化します。'; } else { message.className = 'message-box warning'; message.textContent = '局所SfM候補に対して複数視点最適化を試しましたが、十分に安定した区間を確認できませんでした。'; } window.__360gsBundleResult = { results, good, usable, trackCount, obsCount }; window.dispatchEvent(new CustomEvent('360gs:ba-ready', { detail: window.__360gsBundleResult })); }
async function baRun(detail) { if (baRunning || !detail?.usable?.length) return; const signature = `${baSourceVideo?.currentSrc || baSourceVideo?.src}|${detail.usable.map((r) => `${r.segment.id}-${r.frames.length}-${r.pointCount}`).join('|')}`; if (signature === baLastSignature) return; baLastSignature = signature; baRunning = true; const generation = ++baGeneration, panel = baEnsurePanel(); if (panel) panel.hidden = false; try { const results = []; for (let i = 0; i < detail.usable.length; i += 1) { if (generation !== baGeneration) return; const source = detail.usable[i]; if (baProgressText) baProgressText.textContent = `候補区間 ${i+1}/${detail.usable.length} の全体最適化を行っています`; const optimization = baOptimize(source); results.push({ source, optimization }); await new Promise((resolve) => setTimeout(resolve, 0)); } if (generation !== baGeneration) return; baRender(results); if (baProgressText) baProgressText.textContent = 'カメラ姿勢と疎3D点の全体最適化まで完了しました'; } catch (error) { const panelNow = baEnsurePanel(), message = panelNow?.querySelector('#ba-message'); if (message) { message.hidden = false; message.className = 'message-box warning'; message.textContent = error?.message || '全体最適化を完了できませんでした。'; } if (baProgressText) baProgressText.textContent = '全体最適化を完了できませんでした'; } finally { if (generation === baGeneration) baRunning = false; } }
function baReset() { baGeneration += 1; baRunning = false; baLastSignature = ''; window.__360gsBundleResult = null; const panel = document.querySelector('#ba-panel'); if (panel) panel.hidden = true; }
window.addEventListener('360gs:sfm-ready', (event) => window.setTimeout(() => baRun(event.detail), 150));
baSourceVideo?.addEventListener('loadedmetadata', baReset);
if (window.__360gsLocalSfmResult?.usable?.length) window.setTimeout(() => baRun(window.__360gsLocalSfmResult), 150);
