360GS v0.3c24
Brush runtime loading reliability hotfix after v0.3c23 delivery features
Training baseline remains v0.3c22: direct ERP, spherical geometry, post-triangulation pose refinement, SH1 and effective-rank Gaussian regularization are unchanged
The c22 training.js implementation is left unchanged; an import-map wrapper intercepts only its Brush runtime import
Brush JavaScript is module-preloaded and checked with explicit same-origin HTTP diagnostics before module evaluation
Transient HTTP 429 and 5xx responses are retried once with bounded delay; a failed JavaScript module evaluation/import is retried once with a unique query URL
Brush JavaScript delivery and the 46 MB WebAssembly fetch/initialization are diagnosed separately and preserve detailed errors for the existing training log
The validated c22 Brush JavaScript/WebAssembly runtime remains version-pinned so ordinary browser caching is retained and unnecessary large downloads are avoided
SPZ v4 export, shareable viewpoints and the WebGL 2 viewer remain unchanged from v0.3c23
Build date: 2026-08-21
